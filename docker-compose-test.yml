services:
  db:
    image: postgres:16
    container_name: postgres-test
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: taskdb
    ports:
      - "5433:5432"